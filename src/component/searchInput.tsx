import { InputGroup } from "@/components/ui/input-group";
import { Input } from "@chakra-ui/react";
import React from "react";

const SearchInput = () => {
  return <Input borderRadius={20} placeholder="search gamem..." />;
};

export default SearchInput;
